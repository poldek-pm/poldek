
<variablelist><title>Supported repositories</title>
<varlistentry><term>pndir (read, write, update, incremental update)</term>
  <listitem>
     <para> Native &poldek; index format since version 0.20. Based on
     tndb (cdb-like) DBM library. Consist of 3 database files: first,
     <filename>packages.ndir</filename> with
     basic package informations, second <filename>packages.ndir.dscr</filename> with  
     with common (BuildHost, Distribution, etc) and "C" package descriptions and 
     third, <filename>packages.ndir.dscr.i18n</filename> with all non-English 
     descriptions (if any)</para>
  </listitem>
</varlistentry>

<varlistentry><term>hdrl (read, update)</term>
  <listitem>
     <para> Native &apt-get; and other tools (<application>urpmi</application> AFAIK) 
     index format. Consist of one file (<emphasis>pkglist.%{repository}.bz2</emphasis>)
     with raw, often cut down, RPM package headers.  </para>
     <para> Aliases: apt </para>
  </listitem>
</varlistentry>

<varlistentry><term>yum (read, update)</term>
  <listitem>
    <para> Native &yum; index format.
       Consist of one file with list of packages IDs and directory of *.hdr files
      with raw RPM package headers. 
    </para>
  </listitem>
</varlistentry>

<varlistentry><term>pdir (read, write, update, incremental update)</term>
  <listitem>
    <para>Native &poldek; index format prior to 0.20 version. Based on custom 
    binary format. Consist of one database file (<emphasis>packages.dir</emphasis>) with 
    all needed package header data. </para>
  </listitem>
</varlistentry>

<varlistentry><term>dir (read)</term>
  <listitem>
  <para> Dynamic index built by scanning directory for packages. </para>
  </listitem> 
</varlistentry>
</variablelist>

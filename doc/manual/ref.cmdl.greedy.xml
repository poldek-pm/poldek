<para>
<para>
    Automatically upgrade packages which dependencies are broken
    by uninstalled ones, for example if package foo is upgraded
    from version 1.0 to 1.2 and package foo-devel is already installed
    with requirement "foo = 1.0" then foo-devel will be upgraded 
    to version 1.2 too.
</para>
<para>Configuration option: <option>greedy = [yes|no]</option></para>
</para>
